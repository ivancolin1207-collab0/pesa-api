# ══════════════════════════════════════════════════════════════
#  Servicios PESA API — Dependencias de producción
#  Instalar: pip install -r requirements.txt
#  Render Build Command: pip install -r pesa_api/requirements.txt
# ══════════════════════════════════════════════════════════════

# ── Framework principal ───────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# ── Base de datos async ───────────────────────────────────────
asyncpg>=0.29.0

# ── Seguridad ─────────────────────────────────────────────────
python-jose[cryptography]>=3.3.0
bcrypt>=4.0.0
passlib[bcrypt]>=1.7.4

# ── Validación / configuración ────────────────────────────────
pydantic>=2.7.0
pydantic-settings>=2.3.0
email-validator>=2.2.0
python-dotenv>=1.0.0

# ── Utilidades ────────────────────────────────────────────────
python-multipart>=0.0.9       # Form data en /auth/login

# ══════════════════════════════════════════════════════════════
#  Dependencias de desarrollo / test  (no se instalan en Render)
#  Instalar aparte con: pip install pytest pytest-asyncio httpx
# ══════════════════════════════════════════════════════════════
# pytest>=8.2.0
# pytest-asyncio>=0.23.0
# httpx>=0.27.0
